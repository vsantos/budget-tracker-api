package routes

import (
	"fmt"
	"net/http"
)

// GetCardsEndpoint will return every registered cards from an specific owner
func GetCardsEndpoint(response http.ResponseWriter, request *http.Request) {
	fmt.Println("")
}
